// Init script for Mini-spa

const settings = {
    screenWidth: Math.max(document.documentElement.clientWidth, window.innerWidth || 0), // * .98,
    screenHeight: Math.max(document.documentElement.clientHeight, window.innerHeight || 0) * .75,
    isMobile: true,
}